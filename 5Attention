终于我们要聊到最重要的一关了，attention is all you need.
在transformer架构出现之前，是RNN 循环神经网络，CNN 卷积神经网络 的天下，
RNN专注于读取序列数据（比如文本、语音等），举例子可以说就是一个字一个字的读取，缺点就是不能并行计算，而且上下文一长读着前面忘着后面的内容，
即使针对记忆的问题引入LSTM，也不能完全解决。
CNN 则是专注于图片数据的处理，可以理解是一个一个区块的处理，每个区块都有自己的特征，最后通过全连接层进行分类。

transformer架构则是一个完全基于attention机制的架构，只是利用attention机制来处理序列数据。
可以理解它看文字不是按顺序看，而是按字之间的关系来处理，一瞬间把所有的文字都看完了，这就解决了并行计算的问题。
接下来我们来聊一下transformer架构的详细实现。

首先是文字要转换为token, token 要转换为嵌入向量 embedding vector
每一个token 要有Q，K，V 三个向量。 首先是一个低维度的Wq，Wk 然后对每个token 的原始向量进行计算的得到Q，K。
然后可以想象在一个表格行是K, 列是V, 通过点积之类的操作得到一个代表注意力大小的矩阵。
这个矩阵的每个元素代表了Q和K之间的注意力大小，值越大，代表了Q和K之间的关系越强（为了非线性一般会去掉负数）。
然后进行softmax操作，得到一个概率分布，这个概率分布代表了这句话里面每个tokenQ和K之间的关系。
Wv 是 Wv * 其他词的向量，代表收到其他词的影响。
e = Wv * 初始向量 + 注意力机制代表权重偏移 ，注意是一个矩阵操作，每个token 都有一个权重偏移。
因为在高维空间方向就是语义信息。
总结一下transformer架构的作用就是把各种影响信息，按照权重更新到初始向量，使它丰富，能代表不同的含义。

最后我们看一下论文的公式吧，Attention(Q,K,V) = softmax(Q * K^T / sqrt(d)) * V
其中Q 是查询向量，代表token 在关注的方向。
K 是键向量，代表供其他矩阵发现相关的信息。
V 是初始向量，代表收到影响后的实际信息。
d 缩放因子, 
Q * K^T 是点积操作，注意力示意矩阵，代表所有Q和K之间的关系。

这些是我的理解，关于input embedding , postional encoding , multi-head attention ，残差连接，归一化，位置前馈等训练概念，
需要你进行探索补全，需要参考YouTube上优秀的科普视频。