从Alexnet谈起神经网络
1.什么是权重（weight），权重就是数字，模型学习到的影响力聚显化
2.什么是偏置（bias），偏置就是数字，用于调整神经元的输出。它是一个神经元的默认输出数值
3. 一个神经元的运算可以表示为：
    输出 = 权重 * 输入 + 偏置
   多个神经元可以组成一个层，这个层可以组成一个网络，这个网络可以组成一个模型
   用一个天气预报的例子进行举例，[30,85,60] 分别代表温度，湿度，气温 [-0.02,0.08,0.01] 代表权重，3代表偏置
   那么输出 = -0.02 * 30 + 0.08 * 85 + 0.01 * 60 + 3 = 2.4 这就是一个神经元的简单计算，看起来只有简单的乘法和加法。
   如果只是简单的计算，那么再多个神经元聚合在一起也只是一个线性的计算结果，不能表示复杂的模式。不会因为天气高一度就代表必定会下雨。
   这时候就要引入激活函数（activation function）用Relu函数来举例子，Relu函数的定义是：max(0,x)
   这个函数的作用是将输入的负数映射到0，将非负数映射到本身。这样就可以将线性关系转换为非线性关系，从而表示复杂的模式。可以理解为只要信号强，就输出信号，否则输出0。
4. 让我们回到alexnet的结构，首先是卷积层这里的神经元学习到的是特征图，眼，鼻，嘴巴，眉毛，唇等。这些神经元的结果会被送入下一层，作为输入。
   下面这一层是全连接层，它们会接收卷积层的输出，进行简单的乘法和加法，最后输出一个分类结果。
   这个过程可以重复多次，形成多个层，每个层都有自己的权重和偏置。
   最后一个层是分类层，它会接收所有层的输出，进行简单的乘法和加法，最后输出一个分类结果。
   这样层数越多，学习到的特征就越多，模型能做出的判断也就越精细化。
5. 目前的gpt模型，它的结构和alexnet类似，只是在全连接层之间引入了一些新的层，但是底层也是乘法，加法，激活函数这些基本运算。
6. 这也引出一个问题，这些权重，偏置是怎么学习到的？它们一开始都是随机数，但是通过训练，它们会被更新为更符合数据的数值。下一节的训练将会进行解释。