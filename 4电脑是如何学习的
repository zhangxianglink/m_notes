从上一张我们知道神经元的结果 = 激活函数（（输入 * 权重）+ 偏置）
因为权重是随机的所以结果是对的吗？
这时候就要引入一个损失函数loss function, 他的作用是衡量神经元的结果与目标值之间的差异。
（预测结果-目标值）^2 这就是一个简单的损失函数，平方后可以将负数转换为正数，同时放大差异，加大惩罚力度。
假设我们有一个神经元，他的输入是20，权重是0.5，偏置是1，那么他的结果就是（20 * 0.5）+ 1 = 10
如果目标值是15，那么损失函数就是（10-15）^2 = 25
当知道了差异之后，就可以根据差异来调整权重和偏置，使损失函数最小化，差异越小越好。
差异的调整就是梯度下降是我们的目标，一条曲线，我们的目标就是使这条曲线的斜率最小。（这里应该有条曲线示意图）
25是一个正数，我们应该找到合适的方向进行调整，比如权重增加，偏置减少。或者权重减少，偏置增加。
那么方向应该如何选择？
这里面就引入了一个概念，梯度。gradient
梯度就是我们当前点的斜率，它告诉我们损失函数在当前点的斜率，所以我们可以根据梯度来调整权重和偏置，使损失函数最小化。
（预测值-目标值）* 输入 * 2 用数学的方式来说就是对权重的偏导数。（这个概念需要详细的讲解，让观众看懂）
上面的例子中，预测值是10，目标值是15，输入是20，所以梯度就是（10-15）*20*2 = -100。
这是一个很大的负数，说明权重应该减少。
有了方向，接下来就是调整的力度了，力度应该怎么设置呢？有个专业的名词，叫做学习率learning rate。
学习率是一个很小的数，比如0.01，0.001等。
学习率太多可能直接跳过了斜率最低的地方，导致损失函数最小化失败。学习率太小可能需要很多轮才能收敛。需要根据实际情况来调整。
假设我们的学习率是0.001，新权重就是旧权重 - （学习率 * 梯度）。
所以，上面的例子中，新权重就是0.5 - （0.001 * -100） = 0.6
偏置的结果就是旧偏置 - （学习率 * 梯度）。
新偏置就是1 - （0.001 * -100） = 1.1
预测值就是（20 * 0.6）+ 1.1 = 12.6
损失函数就是（12.6-15）^2 = 5.76
梯度就是（12.6-15）*20*2 = -12. 
从-100 到 -12 肉眼可见的差异减小了，说明权重和偏置的调整方向是正确的。

算一下错了多少（损失函数）
算一下该往哪调（梯度）
调一点点（梯度下降）
然后重复、重复、再重复。

这个”算梯度→调权重→再算梯度→再调权重”的方法，叫做梯度下降（Gradient Descent）。
这只是一个单一的维度，在一个神经网络中，每个神经元都有自己的权重和偏置，所以每个神经元的梯度和权重都要更新。
可以想象在一个多维度的空间里面，同时每个维度都在下降，就像是一个凹凸不平的曲面上有一个球，球会沿着斜率最低的方向下降，直到到达最低点。

那么神经网络是如何一层一层传递信息，进行微调的呢？这就要提到前向传播（forward propagation）和反向传播（backward propagation）。
前向传播是指从输入层开始，一层一层地传递信息，直到输出层。
反向传播是指从输出层开始，一层一层地传递信息，直到输入层。
在前向传播中，每个神经元的结果会被送入下一层，作为输入。
在反向传播中，每个神经元的梯度会被送入上一层，作为输入。 

整体的训练过程就是：
1. 前向传播，计算损失函数，查看误差
2. 反向传播，计算梯度，更新权重和偏置，梯度下降
3. 训练下一条数据，重复以上过程，直到所有数据都被训练过，进入一个epoch。

没有顿悟，有的只是一轮一轮的训练，直到模型的误差最小化。

anthropic,deepseek 都传出过公司的管理层参与到训练的标注中，确保数据的质量和一致性。
而且据说当前人类历史上所有的文本数据都已经训练过了，那么为什么模型的训练会需要这么多的数据呢？
用人类学习举例子，如果是一场突击考试，你已经提前拿到了试卷和答案，那么很显然你只需要完全的背诵答案，就可以通过考试了。
模型也是的，如果训练的数据量太少，模型完全就是在死记硬背，而不是学习。用专业的话来说，这就是过拟合（overfitting）。
还记得之前提到的在alexnet中随机关掉一半的神经元，他们是在迫使模型学习到规律，这也是防止过拟合的一种方法。
当训练的数据质量高，数据量多，超过了神经网络参数的数量，模型就开始学习到数据的规律，而不是背诵数据。
数据就是教材，前向学习就是根据教材学习，loss function就是对答案，反向传播就是老师调整教学方法，Gpu就是计算设备，这是统计学的胜利。






